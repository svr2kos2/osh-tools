# OSH Daemon Configuration
# Copy this file to daemon.toml and modify the values

# Server WebSocket URL
server_url = "wss://your-vps.example.com/bridge"

# Pre-shared secret key (must match server's devices.json)
secret_key = "change-me-in-production"

# Device ID (will be auto-generated by osh-daemon init)
# device_id = ""

# Device name (defaults to hostname if not set)
# device_name = "MyPC"

# Shell to use (optional, auto-detected if not set)
# Windows: "powershell" or "cmd"
# Linux/macOS: "bash" or "sh"
# shell = "powershell"
